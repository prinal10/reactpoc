import React from 'react';
import {Route} from "react-router-dom";

const NavBar = () => {
    return (
        <>
            <Route>

            </Route>
        </>
    );
};

export default NavBar;
