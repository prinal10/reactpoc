import React from 'react';
import Card from "./Card";

const Dashboard = () => {
    //TODO: NAV bar with username on right, logout button, 3 cards on the dashboard
    // console.log("DashBoard")
    return (
        <>
            <Card cardHeader="Dashboard Page" cardContent="This is the where the dashboard page content."/>
            <Card cardHeader="Dashboard Page" cardContent="This is the where the dashboard page content."/>
            <Card cardHeader="Dashboard Page" cardContent="This is the where the dashboard page content."/>
        </>
    );
};

export default Dashboard;
